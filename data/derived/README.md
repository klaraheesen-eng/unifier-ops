# Derived data

Optional folder for **machine-generated summaries** from `raw/` imports (counts, JSON aggregates). **Do not** treat as live CRM.

Regenerate after each intentional `raw/` refresh and note the date in commit messages or in a `SNAPSHOT.md` file here.
